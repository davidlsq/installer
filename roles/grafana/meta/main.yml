---

dependencies:
  - role: lvm
    when: grafana_vg is defined
    vars:
      lvm_vg: '{{ grafana_vg }}'
      lvm_lvs:
        - name: '{{ service_name }}'
          size: '{{ grafana_lv_size }}'
          mount: '{{ service_var_dir }}'
  - role: service
  - role: archive
