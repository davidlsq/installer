---

- name: 'Config "{{ service_etc }}/prometheus.yml" exists'
  ansible.builtin.template:
    src: prometheus.yml.j2
    dest: '{{ service_etc }}/prometheus.yml'
    owner: root
    group: root
    mode: u=rw,go=r
  notify: service reload
