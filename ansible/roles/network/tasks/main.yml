# Package

- name: Package
  ansible.builtin.apt:
    name: libnss-mdns
    state: present

# Config

- name: Config resolv
  ansible.builtin.copy:
    src: resolv.conf
    dest: /etc/resolv.conf
    owner: root
    group: root
    mode: u=rw,go=r
